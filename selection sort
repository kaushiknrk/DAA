#include<stdio.h>
int main() {
   int n,i,j,min,temp;
   printf("Enter no of element:");
    int a[n];
   scanf("%d",&n);
   for(i=0;i<n;i++){
      printf("enter element %d :",i);
      scanf("%d",&a[i]);
   }
   for(i=0;i<=n-2;i++){
      min=i;
      for(j=i+1;j<n;j++){
         if(a[j]<a[min])
            min=j;
      }

   temp=a[i];
   a[i]=a[min];
   a[min]=temp;
   }
   printf("after sorting :");
   for(i=0;i<n;i++){
      printf("%d  ",a[i]);

   }
   printf("\n");
   return 0;
}
